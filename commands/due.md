---
description: What is due in the coming days: bills, EMIs, card dues, planned expenses, reminders
argument-hint: [days, default 30]
---

Show what the user has coming up in Vitafin for the next ${ARGUMENTS:-30} days using the
`upcoming` tool. Present a dated list, one line per item, amounts in their own currency,
with a per-currency total at the end. Do not convert currencies.
